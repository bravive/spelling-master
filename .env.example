# Copy this to .env for local overrides (never commit .env)
# In Railway: set these under Variables in your service dashboard

# Required in production — server refuses to start without this
JWT_SECRET=replace-with-a-long-random-string

# Optional — defaults to 0000 if not set
ADMIN_PIN=0000

# Set automatically by Railway; override only if needed
# PORT=3001
